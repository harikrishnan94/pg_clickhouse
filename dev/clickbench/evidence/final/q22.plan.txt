Limit
  Output: searchphrase, (min(url)), (count(*))
  ->  Sort
        Output: searchphrase, (min(url)), (count(*))
        Sort Key: (count(*)) DESC
        ->  Custom Scan (ClickHouseShmScan)
              Output: searchphrase, (min(url)), (count(*))
