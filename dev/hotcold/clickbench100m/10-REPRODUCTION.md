# Hot/Cold ClickBench-100M — REPRODUCTION

Exact environment, versions, GUCs, ports, load recipe, and the merge template. Mirrors
`dev/wsweep-report/10-REPRODUCTION.md` and `dev/hotcold/phase3/10-REPRODUCTION.md`.

## Environment (verified 2026-06-27/28)
- Host: AWS Graviton aarch64, 32 cores, 61 GiB RAM, disk `/` ~66 GiB free, `/dev/shm` 31 GiB.
- Branch: `streamed-table-shm-offload`. Repo: `/home/ubuntu/pg_clickhouse`.
- PostgreSQL 18, socket `:5432`, `shared_buffers=16GB`, `effective_cache_size=48GB`.
  Access: `sudo -u postgres psql` (peer auth). Extension `pg_clickhouse` (schema `pg`; search_path `pg,public`).
- ClickHouse 26.6.1.1, binary `/home/ubuntu/ClickHouse/build/reldeb/programs/clickhouse`.
  Instance RUN_ID=`tpchcb`, HTTP `:21002`, TCP `:21003`. Resolve the LIVE pid:
  `CHPID=$(ss -ltnp | grep ':21002 ' | grep -oP 'pid=\K[0-9]+' | head -1)`  (manifest CH_PID is stale — D0013).
  FDW server `ch_bench` (PG db `clickbench`) → `127.0.0.1:21002 dbname=clickbench`.
  CH `max_server_memory_usage` ≈ 26.4 GiB (0.9 ratio); `MemoryResidentMax` has reached ~22 GiB. The ~19 GiB seen
  during data-prep was a transient throttle (stale cgroup + memory-tracker-to-free-RAM), NOT the ceiling. With PG
  pinning 16 GiB shared_buffers on a 61 GiB host, the whole-system envelope (16 + 26.4 = 42.4 GiB of ceilings) is
  tight — a global OOM has occurred on this host in a sibling study — so 100M sorts/aggregations REQUIRE external
  spill (`max_bytes_before_external_group_by`, `max_bytes_before_external_sort`); Unit 3 measures real memory.
- cgroup: the CH server was found in a stale sweep cgroup `/pgch_rep_tpch` (cpu.max "800000 100000" = 8 cores);
  uncapped for data prep (`echo max | sudo tee /sys/fs/cgroup/pgch_rep_tpch/cpu.max`). Unit-1 re-establishes the
  shared cpu cap (PG tree + CH) fresh per cell (wsweep_split.sh convention).

## Cold store (CH) — already loaded
- `clickbench.hits_100m` = 99,997,497 rows (14.22 GiB), canonical ClickBench schema, ENGINE=MergeTree
  ORDER BY (CounterID,EventDate,UserID,EventTime,WatchID). Source glob: `hits_{0..99}.parquet` from
  `https://datasets.clickhouse.com/hits_compatible/athena_partitioned/`.
- `clickbench.hits` = 10M (fidelity baseline). `pg.hits` = 10M heap (CH-template-vs-native-PG fidelity at 10M).

## Load recipe (this study), in order
```
# 2) hot subsets in CH (tuple >= B(f); counts verified == N)
bash dev/hotcold/clickbench100m/02_build_hot_ch.sh
# 3) hot heap tables in PG (LIKE pg.hits; COPY ... FROM PROGRAM 'curl ... FORMAT TabSeparated')
bash dev/hotcold/clickbench100m/03_build_hot_pg.sh
# 4) capture deparser-faithful CH bodies for all 43 queries (parallel_workers=0 => single streamed_table)
bash dev/hotcold/clickbench100m/04_capture_templates.sh
# colmap + pure-CH view
curl -s 127.0.0.1:21002/ --data-binary "SELECT position,name,lower(name),type,if(type='DateTime',1,0) \
  FROM system.columns WHERE database='clickbench' AND table='hits_100m' ORDER BY position FORMAT TSV" \
  > dev/hotcold/clickbench100m/colmap.tsv
python3 dev/hotcold/clickbench100m/mk_merge_sql.py ddl | curl -s 127.0.0.1:21002/ --data-binary @-
```
Note: `01_build_hits_idx.sh` (a global-rank build) is a NULL-result artifact — it OOMs at the CH 19 GiB ceiling
and is SUPERSEDED by the boundary-tuple split (D-HC-0403). Kept for the audit trail; do NOT run it.

## Split (D-HC-0403) — exact recency boundary on the UNIQUE 5-tuple
`(EventTime,WatchID,UserID,CounterID,EventDate)` is unique over the 99,997,497 rows. Recency order = all-DESC.
hot(f) = `tuple >= B(f)`, cold(f) = `tuple < B(f)`; B(f) (the N(f)-th most-recent row) in `boundaries.env`.
N(1%)=1,000,000 N(5%)=5,000,000 N(10%)=10,000,000. Verified EXACT (hot==N, hot+cold==TOTAL).

## Merge template (D-HC-0402) — single CH execution, no planner routing
1. Background producer (PG): streams the hot heap into a named SHM ring and waits for the CH consumer:
```
sudo -u postgres psql -d clickbench -X -q -c \
 "LOAD 'pg_clickhouse'; SET search_path=pg,public; SET statement_timeout='240s';
  SELECT clickhouse_stream_relation('pg.hits_hot_p<f>'::regclass, 'pgch_hot_p<f>', 65536);" &
# wait for /tmp/clickhouse_shm_pgch_hot_p<f>.sock
```
2. Merged query (CH HTTP), the per-query deparsed body with its single streamed_table source replaced by
   the hot∪cold subquery (generated by mk_merge_sql.py):
```
SELECT <body> FROM (SELECT * FROM (
    (SELECT <projcols> FROM streamed_table('pgch_hot_p<f>', '<FULL 105-col wire schema>'))
    UNION ALL
    (SELECT <projcols, cold-cast> FROM clickbench.hits_100m WHERE <tuple < B(f)>)
)) [WHERE..GROUP BY..ORDER BY..LIMIT]
```
   CH session settings (match the deparser's; identical for pure-CH and merge):
   `allow_experimental_streamed_table_function=1, join_use_nulls=1, group_by_use_nulls=1, final=1,
    count_distinct_implementation=uniqExact, max_bytes_before_external_group_by=4G, max_bytes_before_external_sort=4G`.

- pure-CH-100M baseline (ground truth): same body, source = `clickbench.hits_dt64`
  (a view = hits_100m with lowercase names + the 3 timestamps cast to DateTime64(6,'UTC')).

Generate either with: `python3 mk_merge_sql.py templates/q<q>.ch.sql {pure|p01|p05|p10}`.

## Correctness oracle
```
bash dev/hotcold/clickbench100m/05_oracle.sh          # all eligible q × {p01,p05,p10}
# or a subset:  FRACS="p01" QLIST="5 18 23 29" bash dev/hotcold/clickbench100m/05_oracle.sh
```
Per (f,q): cmp_results.py(pure, merge) ∈ {exact,float,approx} AND producer returned N(f) rows AND
ShmAdoptedBlocks+ShmCopiedBlocks ≥ 1 (poll-retry for the async query_log flush). Plus partition exactness per f
and a teardown leak check. Results: `results/oracle/summary.tsv`.

## Eligible query set
42/43 ClickBench queries (templates/q{2..43}.ch.sql). Q1 (`SELECT COUNT(*)`) is INELIGIBLE — it references no
column, so the planner declines offload (no streamed_table); same intentional decline as the full-offload study (D0010).
